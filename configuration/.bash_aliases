# Set aliases.
alias lst='ls -lah'
alias mkd='mkdir -pv'
alias gfs='git fetch && git status'

# Set group write permissions.
umask 0002

# Set environment variables.
export DATABASE_USER=pimbox
export DATABASE_PASSWORD=secret
